Future<void> downloadBytes({
  required List<int> bytes,
  required String fileName,
  required String mimeType,
}) async {
  throw UnsupportedError(
    'La descarga de archivos esta disponible desde el panel web.',
  );
}
